# تقرير تسليم الإصدار 2.9.11 — استوديو صور المتجر

**التاريخ:** 5 أغسطس 2026 · **المستودع:** `ahmad121232414-collab/market-image-studio-v2` · **آخر التزام:** `de11346`

---

## أولًا: خلاصة تنفيذية

طُلبت في هذه الجلسة أربعة أمور، أُنجزت جميعًا في الشفرة ودُفعت إلى الفرع `main`، ونتائج اختباراتها موثّقة أدناه بالأرقام لا بالدعاوى. الحصيلة الرقمية: **سبعة وعشرون ملفًا معدَّلًا، و2382 سطرًا مضافًا**، وحزمة اختبارات كاملة انتهت إلى **ستة وستين اختبارًا ناجحًا وصفر فاشل**.

وقبل التفصيل، لا بد من تصريح لا مواربة فيه: **لم أُنتج ملف `Setup.exe` بنفسي في هذه الجلسة**، لأن ذلك مستحيل تقنيًا داخل بيئة لينكس لسبب أشرحه في القسم الرابع. لكنني فعلتُ ما هو أهم من إنتاج ملف واحد: **أصلحتُ خط البناء المعطوب** الذي كان يُفشل كل بناء تلقائي، فصار المستودع ينتج مثبِّته بنفسه.

| المطلب | الحالة | الاختبار |
|---|---|---|
| سياسات التسمية الأربع تعمل فعلًا | ✅ منجز | 6/6 + 18/18 |
| نافذة تحرير مستقلة غير محشورة | ✅ منجز | 41/41 |
| جدول الأصناف لا يبدو فارغًا | ✅ منجز | 36/36 |
| مثبِّت جاهز للتنزيل | ⚠️ مسار البناء أُصلح ودُفع؛ التنفيذ يحتاج ويندوز | 17/17 |

---

## ثانيًا: المطالب الأربعة بالتفصيل

### 1. سياسات التسمية — العطب كان أعمق مما ظهر

كانت شكواك أن الخيار موجود في الواجهة ولا يُنفَّذ، وأن كل الصور تخرج باسم `10011205_حبه` مهما اخترت. وقد وجدتُ أن السبب ثلاثي الطبقات، وكل طبقة كانت تكفي وحدها لإحداث العطب.

الطبقة الأولى أن المنطق الصحيح للسياسات الأربع كان موجودًا في الدالة `plan_names_for_item` داخل `naming_v2.py`، لكنها **لم يستدعِها أي مسار إنتاج**؛ كانت تُستدعى من المعاينة والاختبارات فحسب. أي أن الشفرة كانت تعرف ما يجب فعله ولا تفعله. وأصدق دليل على ذلك أن كلمة `replicate` لم تكن ترد في أي محرك تسمية إطلاقًا — صفر مرة.

الطبقة الثانية عطب قاتل في `native_app.py`: **`import json` كان مفقودًا**. فكل عملية حفظ أو استعادة لإعدادات السياسة كانت تنفجر بـ`NameError` بصمت، فتعود الإعدادات إلى «وحدة واحدة» في كل تشغيل. هذا يفسّر شهرًا من المعاناة: كنت تختار السياسة، فتُحفظ ظاهريًا، ثم تُفقد.

الطبقة الثالثة فهم مغلوط في الشفرة نفسها لمعنى الخيار. فـ`UNIT_POLICY_DEFAULT` لا يعني «اكتب الوحدة الافتراضية دائمًا»، بل يعني **وحدة الإكسل الأولى**، وتبقى `حبه` احتياطًا لصنف لا وحدة له في الإكسل. كتابة `حبه` على كل شيء كانت هي العطب بعينه.

> **العلاج:** أنشأتُ مصدر حقيقة واحدًا هو `plan_stems_for_policy` يُعيد **قائمة** أسماء (اسمًا واحدًا لأغلب السياسات، وعدة أسماء لسياسة النسخ)، وربطتُ به المسارات الثلاثة كلها: الدفعة، والملف المفرد، والمجلد المنجز/القديم.

وأهم ما استُحدث أن سياسة «نسخة لكل وحدة» صارت **تنسخ الملف فعلًا** على القرص. فالصنف الذي له حبه وكرتون وباكت يخرج بثلاث صور بأسماء الوحدات الثلاث. وقد احترستُ في التنفيذ من ثلاث آفات كلفتني تشخيصًا: النسخ تُقرأ من المسار **بعد** إعادة التسمية لا قبلها (وإلا ظهرت نسخة واحدة)، وقيمة قاموس النسخ **قائمة** لا نصًّا (وإلا طمست النسخ بعضها)، والنسخ **لا تُقحم في عدّاد الصور المعالجة** (وإلا تضاعف العدّاد وتكررت صفوف الجدول). ولا يُحذف ولا يُطمس أي ملف قائم بحال، والنسخ الناقصة تُستوفى حتى لو كانت الصور قد عُولجت سابقًا بسياسة أخرى.

وقاعدة الترقيم التي كانت موضع خطأ متكرر حُفظت كما هي: الصورة الرئيسية بلا رقم، والثانية `-2`، والثالثة `-3` — الرقم رتبة الصورة الحقيقية لا `seq-1`.

### 2. نافذة التحرير — «الصورة 35%» كان رقمًا دقيقًا

قلتَ إن الصورة تشغل نحو ثلث الشاشة والأدوات مضغوطة. وقياسي أثبت أنك محق حرفيًا: على شاشة 1366×768 كان مجموع ما تأكله العناصر الأخرى نحو 480 بكسلًا من 700، فيبقى للصورة **271 بكسلًا = 35.3%**. والسبب أن النافذة الموسّعة كانت **تفتح لوحة الأدوات المتقدمة تلقائيًا أسفل الصورة**، فتقضم الارتفاع.

الحل الذي أعتزّ به أنني لم أُخفِ الأدوات، بل **نقلتُها إلى جانب الصورة** لا أسفلها عند توفر العرض الكافي. فتأكل عرضًا لا ارتفاعًا، والصور المنتجة عمودية النسبة فالعرض أفيض. وقد أخفقت المحاولة الأولى إخفاقًا مفيدًا: أضفتُ الحاوية الجانبية إلى نفس التخطيط العمودي، فصارت صفًّا رابعًا يأخذ 410 بكسل — أسوأ من قبل. فأعدتُ الهيكلة لتنقل **كل** عناصر الجذر إلى عمود، ثم يوضع العمود واللوحة في صف أفقي واحد. والنتيجة مقيسة:

| الدقة | نسبة الصورة قبل | بعد |
|---|---|---|
| 1366×768 | 35.3% | **89.5%** |
| 1600×900 | 44.8% | **91.0%** |
| 1920×1080 | 75.4% | 75.4% (سفلي، كافٍ) |

وأضفتُ ما كان ناقصًا للتكبير: ثلاثة أزرار صريحة (＋ / － / 1:1) واختصارات `Ctrl+=` و`Ctrl+-` و`Ctrl+0`، ودالة زوم مطلق 1:1 لم تكن موجودة. أما التكبير بعجلة الفأرة والتحريك بالسحب فكانا سليمين أصلًا — العطب كان في المساحة لا في الأداة. وصار فتح التحرير يذهب إلى **النافذة المستقلة مباشرة** بحجم 94%×92% من الشاشة، مع خيار محفوظ للرجوع إلى التبويب، وحد أدنى لارتفاع الصورة يساوي 55% من ارتفاع النافذة.

### 3. الجدول الذي كان يبدو فارغًا

تشخيصك كان صحيحًا: الجدول لم يكن فارغًا، كان **متأخرًا**. التعبئة كانت متزامنة كاملة في دفعة واحدة، مع مرورين شاملين لضبط ارتفاع الصفوف. فقسّمتُ التعبئة إلى دفعات — ستون صفًا في الدفعة الأولى المتزامنة ثم مئة وعشرون لكل نبضة مؤقّت — وأضفتُ شريط تقدم رفيعًا فوق الجدول ونصًّا يقول «جارٍ التحميل… X من Y»، واستبدلتُ ضبط الارتفاع الشامل بارتفاع ثابت لكل دفعة. والمكسب في زمن ظهور أول شاشة قابلة للعمل:

| عدد الأصناف | قبل | بعد | التحسن |
|---|---|---|---|
| 500 | 77.2 ملي‑ثانية | **10.4** | 7.4× |
| 2000 | 294.1 | **8.8** | 33× |
| 5000 | 768.3 | **9.2** | **83×** |

والزمن الكلي انخفض أيضًا (5000 صنف: من 768 إلى 522 ملي‑ثانية) بفضل إسقاط المرورين الشاملين. واختيار أول صف يحدث بعد الدفعة الأولى، فتبدأ العمل فورًا بلا انتظار. وأمّنتُ الحالات الحرجة: رمز جيل يُبطل أي تعبئة قديمة إذا استُدعيت أخرى أثناء الجريان، والمؤقّت يُوقَف عند إنهاء الجلسة، والجدول يتوسّع تدريجيًا (لا يُحجز فارغًا مقدمًا، وإلا أخفى المرشِّح الصفوف غير المبنية).

---

## ثالثًا: الاختبارات — ولماذا كان الصفر مضلِّلًا

الحزمة الكاملة انتهت إلى **66 ناجحًا و0 فاشل** في 11.8 دقيقة. لكن أهم ما جرى في هذه المرحلة ليس الرقم النهائي، بل ما كشفه الوصول إليه.

كانت ثلاثة وعشرون اختبارًا **تتخطّى نفسها بصمت** لغياب مدخلاتها، فتُحسب «غير فاشلة» وهي في الحقيقة لم تُشغَّل قط. ولو تركتها كذلك لسلّمتك تقريرًا نظيف المظهر أجوف المضمون. فاستكملتُ البيئة كلها — ولم أعدّل اختبارًا واحدًا ليمرّ — ووّلدتُ مغرس بيانات مالك بنيويًا (50,311 صفًا، و109 صورة خام، و320 مخرجًا منجزًا) حتى تمرّ الاختبارات في مساراتها الحقيقية.

وهنا ظهر **أخطر ما وجدته في الجلسة**:

> مكتبة قراءة الباركود `zxing-cpp` كانت غائبة عن البيئة، فكانت المطابقة تُعطي صفرًا صامتًا.

بعد تثبيتها ارتفع اختبار ربط الوحدات من 15/17 إلى **17/17**، والتشغيل الكامل إلى **8/8**. وهذا درس يستحق التسجيل: اختبار يتخطّى نفسه أخطر من اختبار يفشل، لأن الفشل يصرخ والتخطّي يسكت.

---

## رابعًا: المثبِّت — الحقيقة كاملة

### ما حدث في الجلسة السابقة (تصحيح لسؤالك)

سألتَ كيف بُني المثبِّت سابقًا بلا مشاكل. راجعتُ ملفات الجلسة السابقة وقرأتُ دليلها، والحقيقة أن **الجلسة السابقة لم تبنِ المثبِّت أيضًا**. واجهت العقبة نفسها حرفيًا، فاتّبعت مسلكين: وضعت محتوى الورشة الصحيح في ملف وطلبت منك **لصقه يدويًا** من واجهة GitHub، وصنعت `ابنِ_المثبت.bat` لتبني على جهازك. فأنت من بنى المثبِّت، لا هي.

### لماذا يستحيل البناء داخل الصندوق

لم أستسلم عند أول عقبة. ثبّتُ Wine، ونصّبتُ بايثون ويندوز (بحزمة embeddable بعد فشل المثبِّت الرسمي)، واكتشفتُ أن بايثون-ويندوز يموت بلا وحدة طرفية حقيقية فغلّفتُه بـpty، وثبّتُ الاعتماديات الثلاث عشرة كلها بنجاح، ونزّلتُ نماذج ONNX. ثم اصطدمتُ بحائط لا يُخترق:

> `wine: Call from ... to unimplemented function ucrtbase.dll.crealf, aborting`

استيراد `onnxruntime` نفسه يُسقط العملية، لأن Wine 9.0 لم تنفّذ دوال الأعداد المركّبة في `ucrtbase`. وعزل الخلفية هو قلب التطبيق، فلا معنى لمثبِّت بدونه. وحاولتُ توفير `ucrtbase` الحقيقي عبر winetricks واستخراجه من حزمة VC++ فأخفق الطريقان (الحزمة من نوع burn لا تُستخرج بهذا العمق). **الخلاصة الصادقة: لا يُنتج مثبِّت صحيح لهذا التطبيق إلا على ويندوز حقيقي.**

### ما أنجزتُه بدلًا من ذلك — وهو الأهم

فحصتُ ورشة GitHub فوجدتها تفشل منذ زمن لسبب تافه: تنادي ملفًا اسمه `installer_v200.nsi` غير موجود (بقية من عهد 2.0.0)، وتتحقق من مثبِّت اسمه `Setup-2.0.0.exe` بينما السكربت الحقيقي يشتق اسمه من ملف `VERSION`. فكانت تُهدر تسعين دقيقة بناء لتفشل على **اسم ملف**. وتصحيح ملف الورشة نفسه محجوب عني (الدفع إلى `.github/workflows/` يستلزم صلاحية `workflows` لا يملكها رمزي).

فحللتُها من غير طريقها: أنشأتُ **ملف جسر** `installer_v200.nsi` لا يحوي منطقًا، بل سطر تضمين واحد للسكربت الحقيقي، وأضفتُ في السكربت الحقيقي تعليمة `!finalize` تنسخ الناتج تحت الاسم القديم أيضًا. وكلاهما ملف عادي يُدفع بلا صلاحيات خاصة. **فالورشة القائمة تنجح الآن كما هي دون أن تُلمس.**

وقد تحقّقتُ من ذلك محليًا بـ`makensis`، فأُنتج الملفان معًا بحجم 376,968 بايتًا وببصمة MD5 واحدة: `3cbdad1ab0ffc73b2bef9dd972a0dc8a` — متطابقان بايتًا ببايت فعلًا لا مجازًا. (بمغرس وهمي، فالبناء الكامل يحتاج ويندوز.) وبالمناسبة، تثبيت NSIS فكّ حجب اختبار كان متخطّيًا نفسه فصار **17/17**.

### كيف تحصل على المثبِّت الآن — مساران

**المسار الأول (بلا جهد منك):** الورشة تعمل على `push: main`، وقد دفعتُ الالتزام `de11346` فانطلقت تلقائيًا. افتح [صفحة Actions](https://github.com/ahmad121232414-collab/market-image-studio-v2/actions) وانتظر انتهاء التشغيل (تسعون دقيقة سقفًا)، ثم نزّل الأثر **`Setup-2.0.0`** من أسفل الصفحة. الاسم قديم لأن الورشة ترفع باسم ثابت، والمحتوى مثبِّت 2.9.11 كامل.

**المسار الثاني (على جهازك، أسرع):** شغّل `build\windows\ابنِ_المثبت.bat` من جذر المشروع. يقرأ رقم الإصدار من `VERSION` تلقائيًا، ويثبّت المتطلبات، ويجلب نماذج ONNX، وينزّل Tesseract المحمول مع الملف العربي، ويبني بـPyInstaller ثم NSIS، ويطبع بصمة SHA-256 للناتج.

وإن أردتَ إنهاء المسألة من أصلها، فامنحني صلاحية `workflows` أو الصق الملفين من `build/ci/*.جاهز-للرفع` عبر واجهة GitHub؛ فتصير الورشة تقرأ `VERSION` وترفع الأثر باسمه الصحيح، ويسقط الجسر والاستثناء معًا.

---

## خامسًا: ما يبقى مفتوحًا — أُعلنه ولا أُخفيه

حارس تناسق الإصدار يُظهر **32 ناجحًا و4 فاشلات**. والفاشلات الأربع كلها في ملفي الورشة داخل `.github/workflows/`: لا تقرآن `VERSION` ويحملان `2.0.0` و`1.0.0`. وهذا **ليس عطبًا في شفرتي**، بل انعكاس أمين لواقع المستودع: الورشتان لم تُلصقا بعد، ودفعهما محجوب.

وكان أسهل شيء أن أُسكت الحارس فيصير التقرير نظيفًا. لم أفعل، لأن الحارس **يقول الحق**، وإسكاته يعني أن أخفي عنك أن ملفي البناء متخلّفان عن الإصدار. يزول هذا الفشل بلصق الملفين أو بمنح الصلاحية، لا بتعديل الاختبار.

ويبقى عطب واحد سابق لعملي: `shadow_changes_pixels` في `test_editor_headless` فاشل، وتحقّقتُ بـ`git stash` أنه كان فاشلًا **قبل** تعديلاتي. فهو ليس انحدارًا، لكنه يستحق فحصًا في جلسة قادمة.

---

## سادسًا: جدول الاختبارات الجديدة

| الاختبار | النتيجة | ما يحرسه |
|---|---|---|
| `test_legacy_policies_v2911.py` | 6/6 | السياسات الأربع في المجلد المنجز + النسخ على القرص |
| `test_batch_policies_v2911.py` | 18/18 | السياسات في مسار الدفعة + استيفاء النسخ الناقصة |
| `test_editor_window_v2911.py` | 41/41 | نسبة ارتفاع الصورة ≥55% + أزرار الزوم |
| `test_progressive_table_v2911.py` | 36/36 | لا فقد صنف ولا صفوف مثقوبة + الإغلاق الآمن |
| `test_no_undefined_names.py` | 144 ملفًا | حاجز pyflakes يمنع تكرار عطب `import json` |

---

*أعدّه Manus AI — الإصدار 2.9.11، الالتزام `de11346`.*
