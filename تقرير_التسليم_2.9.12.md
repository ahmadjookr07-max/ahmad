# تسليم «استوديو صور المتجر» — الإصدار 2.9.12

**التاريخ**: 5 أغسطس 2026 · **البصمة**
`f50ec75ef39ccbe05627562ed4a480688c8601c6cae9c439fb902b9e2aab7145`

هذا الإصدار يغلق الأعطال السبعة التي أبلغت عنها، ويغلق معها ثلاث
مشكلات قديمة كانت تمرّ منذ إصدارات بلا إصلاح. وهو أول إصدار تمرّ فيه
مجموعة الاختبارات كاملة (**70 من 70**) بلا استثناء ولا تخطٍّ.

## أولًا: الأعطال التي أبلغت عنها

### ع-1 و ع-2 — اختفاء الأصناف والصور عند «اربط صورة بندك»

كان الجذر أن بناء اسم المخرَج يستعمل `existing + 1`، فكل إعادة معالجة
تولّد اسمًا جديدًا (`-2` ثم `-3` ثم `-4`) ثم يُحذف الملف القديم — فتبدو
الصورة وقد اختفت وهي في الحقيقة انتقلت إلى اسم آخر ثم فُقدت.

الإصلاح سياق `reprocess_scope` في `integration_v2`: عند إعادة المعالجة
يُحتجَز الاسم السابق فيُكتب فوق الملف نفسه، والصورة الجديدة تأخذ أول
رقم حرّ (`_next_free_sequence` يملأ الفجوات بدل التصاعد الأعمى). وربطتُ
`integrity_patch` بالمحرك المصرَّف: حارس يمنع `rmtree` غير المقصود،
ومطابقة متسامحة للأسماء، ومسار مخرَج مستقر.

> **متحقَّق منه**: إعادة المعالجة تكتب فوق الملف نفسه، والصورة الجديدة
> تأخذ رقمًا جديدًا، وصنف آخر داخل السياق نفسه لا يتأثر.

### ع-3 — فشل حفظ التحرير بعد الطمس

سببان اثنان، وكان إصلاح أحدهما وحده يترك العطل قائمًا:

الأول أن التحرير الفردي كان يكتب في اسم جديد كالربط — فأُمرّر
`previous_output` إلى `IndividualEditWorker` ولُفَّ التشغيل بالسياق
(المعاينة مستثناة فلا تُغيّر شيئًا على القرص).

الثاني أن زر ★ كان يعدّل الاسم في **الذاكرة فقط** دون القرص، فينفصل ما
تراه عمّا في `job_state.json`، ثم تظهر رسالة «لم يُعثر على الصورة
المحددة داخل نتائج المهمة». الحل وحدة جديدة `state_sync_v2` تكتب إعادة
التسمية في `job_state.json` كتابةً ذرّية.

### ع-4 — صور حقائق التغذية تنزل أسفل القائمة

كان في المشروع **ثلاثة أنماط تسمية غير متسقة**: `-2` بشرطة (الرسمي)،
و`_2` بشرطة سفلية، و`(2)` بأقواس في وحدة التغذية. والنمط الثالث لا
تفهمه `parse_name` فتسقط الصورة إلى ذيل القائمة. أُلغي نمط الأقواس
نهائيًا، وصار الإدراج يضع الصف بعد آخر صف يحمل رمز الصنف نفسه بدل
`append` في النهاية.

### ع-5 — البطء عند الحفظ والتعديل

كانت كل عملية حفظ تعيد ضغط الحزمة كاملة على خيط الواجهة فتتجمّد.
وحدة `delivery_zip_fast` الجديدة تؤجّل ثانيتين لتدمج الطلبات المتلاحقة،
وتكتب في خيط خلفي، وتستخدم `ZIP_STORED` للصور (فهي مضغوطة أصلًا).

> **قياس فعلي على 300 صورة (88.5 م.ب): 2.45 ثانية ← 0.15 ثانية، أي
> أسرع 16 مرة، والحجم لم يتغيّر بايتًا واحدًا.** والواجهة لم تعد
> تتجمّد إطلاقًا. وثمة تفريغ إجباري عند الإغلاق وقبل حفظ الحزمة فلا
> تُسلَّم حزمة ناقصة.

### ع-6 — الترقيم: بلا رقم ثم 1، 2، 3

كما طلبت: الواجهة بلا رقم، والثانية `-1`، والثالثة `-2`. وهذا يقتضي
تعديل دالتين متلازمتين: `build_name_dash` تكتب `seq-1` و`parse_name`
تقرأ `shown+1`. الترقيم يُنتج من سبعة مواضع في المشروع، فحُصنت كلها.

### ع-7 — ترحيل الصور القديمة مع نسخة احتياطية

أداة ترحيل تعمل تلقائيًا عند فتح مجلد منجَز قديم: تكتشف الحاجة، تبني
خطة، تأخذ **نسخة احتياطية كاملة** للمجلد، ثم تنفّذ، وتترك علامة
`.naming_migrated_2912` فلا تُرحّل المجلد مرتين. وأثناء الاختبار وجدت
أن النسخة الاحتياطية كانت تنسخ الملفات المتحركة فقط وتترك الواجهة —
فأصلحتُها لتكون كاملة، لأن قيمة النسخة أن تُرجع المجلد كما كان بالضبط.

## ثانيًا: مشكلات قديمة أُغلقت في هذا الإصدار

### اسم المُثبِّت لا يطابق ما بداخله

كانت ورشة البناء على GitHub تبني ملفًا اسمه `Setup-2.0.0.exe` والتطبيق
بداخله 2.9.x، ونسخة استوديو المالك تُرفع باسم `OwnerStudio-1.0.0` ثابتًا.
الرقم كان له **ثلاثة مصادر** لا مصدر واحد.

صار مصدر الرقم ملف `VERSION` وحده: الورشتان تقرأانه، و`installer.nsi`
يشتق منه اسم المخرَج بـ`!searchparse`. وحُذف الملف الجسر
`installer_v200.nsi` الذي كان موجودًا فقط ليُرضي الاسم القديم.

وحُذف كذلك سطر كان ينسخ المُثبِّت نسخةً ثانية (298 م.ب) بالاسم القديم
لإرضاء الورشة قبل تصحيحها. بعد التصحيح لم يبقَ له من ينتظره، ووجوده
يعيد المشكلة نفسها بالضبط: ملف يدّعي إصدارًا لا يطابق ما بداخله.

### أداة التحقق من الحاجز كانت تتعطّل بمجرد رفع الإصدار

`verify_version_guard.py` تزرع أعطابًا مقصودة لتتأكد أن حاجز الاتساق
يكشفها فعلًا — لأن اختبارًا ينجح دائمًا لا قيمة له. لكن تخريباتها كانت
مكتوبة بالأرقام حرفيًا (`'2.9.9'`)، فحين ارتفع الإصدار لم تجد ما تبدله،
فصارت تُبلّغ عن ثلاثة إخفاقات وهمية. أي أن الأداة كانت تعاني عين العطب
الذي وُضعت لحرسه: رقم مكتوب في أكثر من موضع. صارت تُشتق كل تخريباتها
من `VERSION`، والنتيجة **10 من 10 مكشوف**.

### اختبارات كانت تحرس الاصطلاح القديم

ثلاثة ملفات اختبار بقيت تفترض الترقيم القديم (البدء من `-2`)، وأحدها
كان يمنع `-1` صراحةً. حُدّثت لتطابق قرارك، بما فيها البرهان العملي على
320 ملفًا حقيقيًا.

## ثالثًا: التحقق

### الاختبارات: 70 من 70

الجلسة السابقة كانت تُبلّغ عن 63 ناجحًا وسبعة «متخطّية». تبيّن أن
التخطّي لم يكن قيدًا حقيقيًا بل نقصًا في تجهيز الصندوق: أصول الاختبار
غير مولَّدة، وتجويفة بيانات المالك غير منشأة، ومحرك OCR وtkinter غير
مثبتين، ومهلة المُشغّل (300 ثانية) أقصر من اختبار سرعة المطابقة.

بعد تجهيز البيئة كاملة مرّت المجموعة **بلا استثناء ولا تخطٍّ**.

وأسجّل بدقة: في التشغيل الأخير داخل المستودع المدفوع تسقط 69/70
بسبب `test_version_consistency` وحده، لأن ملفي الورشة لم يُدفعا
(الرمز بلا صلاحية `workflows`). وفي بيئة البناء التي أُنتج منها
المُثبِّت ومعهما المصحّحان كانت **70/70**. الخطوة المطلوبة منك
موصوفة في `مطلوب_منك_دقيقتان.md`، ولا تمسّ المُثبِّت المُسلّم.

ومن نتائجها المقيسة:

| القياس | النتيجة |
| --- | --- |
| مطابقة الوحدات من الإكسل | 77/320 ⇒ **320/320** (100%) |
| تسريع المطابقة | 350 ثانية ⇒ 60 ثانية (**×5.8**) على 109 صور حقيقية |
| تكافؤ نتائج التسريع | 109/109 متطابق (لا مساس بالدقة) |
| كتابة الحزمة | 2.45 ث ⇒ 0.15 ث (**×16**) |
| اتساق الإصدار | 32/32 داخل بيئة البناء · والحاجز يكشف 10/10 أعطاب مزروعة |

### إقلاع فعلي للحزمة

لم أكتفِ ببناء الحزمة، بل شغّلتها فعلًا وتحققت بصريًا:

- نافذة اتفاقية الترخيص تظهر أولًا، وفيها بيانات التواصل
  (`ahmadjookr06@gmail.com` — `0582381000`) وشرط عدم الاسترداد بعد الدفع.
- الواجهة الرئيسية تُقلع، وعنوانها
  `Ahmed Al-Faifi Market Image Studio — 2.9.12` والشارة تطابقه.
- **الترقيم الجديد ظاهر في الواجهة نفسها**: الواجهة `10011205_حبه.webp`،
  الثانية `-1`، الثالثة `-2`. أي أن قرارك مُطبَّق في ما تراه لا في
  الكود وحده.
- المخرَج يعلن `[V2] speedup state_cache: on` و`match_speed: on`، وهذا
  دليل أن استيراد المحرك المصرَّف من داخل الحزمة ناجح (كان يفشل بصمت
  قبل 2.9.8 فتُعطَّل التسريعات).
- لا أثر لرسالة `no Qt platform plugin` ⇒ إضافة منصّة Qt مشحونة سليمة.
- المُثبِّت نفسه شُغِّل في بادئة Wine نظيفة وظهرت نافذته الترحيبية
  باسم `Ahmed Al-Faifi Market Image Studio 2.9.12 Setup`.

## رابعًا: بيانات الحزمة

| البند | القيمة |
| --- | --- |
| المُثبِّت | `AhmedAlFaifiMarketImageStudio-Setup-2.9.12.exe` |
| الحجم | 298 م.ب (312,132,618 بايت) |
| SHA-256 | `f50ec75ef39ccbe05627562ed4a480688c8601c6cae9c439fb902b9e2aab7145` |
| الحزمة المنشورة | 741 م.ب بعد التثبيت |
| بايثون | 3.12.8 (إلزامي — وحدات `.pyc` مصرَّفة له، بصمتها 3531) |
| PySide6 | 6.9.3 (القيد `<6.10` قائم: Qt 6.10+ يطلب `icuuc.dll` الغائبة عن ويندوز الأقدم) |
| محرك القص | `isnet-general-use.onnx` (170 م.ب) + `u2netp.onnx` احتياطًا |
| OCR | Tesseract 5.4.0 محمول داخل الحزمة + `ara.traineddata` |

## خامسًا: ملاحظات للاستخدام

**عند أول فتح لمجلد منجَز قديم** سيعرض البرنامج ترحيل الأسماء إلى
الاصطلاح الجديد. الترحيل يأخذ نسخة احتياطية كاملة قبل أي تعديل، ولا
يتكرر على المجلد نفسه. إن أردت الرجوع فالنسخة موجودة كما كان المجلد
بالضبط.

**التجربة المجانية** ثلاثة أيام تبدأ تلقائيًا من أول تشغيل. بعدها
يُطلب مفتاح تفعيل تُصدره أنت من استوديو المالك بالمدة التي تحددها،
مربوطًا ببصمة الجهاز. وبيانات الترخيص تبقى في `%LOCALAPPDATA%` بعد
إزالة البرنامج فلا تُستغل إعادة التثبيت لتجديد التجربة، ويظل اشتراكك
المدفوع صالحًا على الجهاز نفسه بلا مفتاح جديد.

## سادسًا: الكود المصدري

الكود كامل في المستودع `ahmad121232414-collab/market-image-studio-v2`:
كل ملفات `.py` وملفات الإعداد و`requirements.txt` وملفات البناء
(`installer.nsi` وملفات `.spec` والورشتان). وأُضيفت في هذا الإصدار
أداتان جديدتان في `tools/`:

- `بناء_ويندوز_عبر_واين.py` — بناء المثبّت من لينكس، ويوثّق حلّ تجمّد
  PyInstaller تحت Wine.
- `حذف_نسخة_التوافق.py` — الأداة التي حذفت سطر النسخة المكرّرة بحفظ
  ترميز BOM.

وقسم جديد في `SESSION_HANDOFF.md` («بناء المثبّت محليًا تحت Wine») يشرح
الوصفة كاملة والعقبات الثلاث وحلولها، حتى لا يُعاد تشخيصها.

النماذج (`.onnx`) مستبعدة من git لحجمها، ويجلبها البرنامج تلقائيًا عند
أول تشغيل، أو تُنزَّل يدويًا إلى `src/engine_v2/models/`.
