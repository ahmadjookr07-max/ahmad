# تقرير التسليم — استوديو صور المتجر، الإصدار 2.9.5

**المالك:** أحمد الفيفي — ahmadjookr06@gmail.com — 0582381000
**تاريخ التسليم:** 3 أغسطس 2026
**حالة النسخة:** مستقرة ومُختبَرة، جاهزة لبناء المُثبِّت على ويندوز

---

## أولًا: الخلاصة التنفيذية

البرنامج يُسلَّم في حالة **مستقرة مُثبَتة بالقياس لا بالانطباع**: الفحص
الشامل يمرّ بنتيجة **48 من 48 ملف اختبار**، صفر فشل، في 196 ثانية.
والنسخة المُسلَّمة هي النسخة العاملة المُختبَرة نفسها، **لا** نسخة
نصف مُرقَّعة — إذ لم تُطبَّق أي رقعة تلقائية على كود البرنامج، وظلّ
الجرّاح في وضع المعاينة طوال الجلسة.

أهم ما تحقّق في هذه الجلسة ليس إضافة ميزة، بل **إصلاح أربع علل كانت
ستمنع التسليم أو تُفسده بصمت**. ثلاث منها في بيئة تحقق الجرّاح،
والرابعة في خط بناء ويندوز — وهذه الأخيرة كانت ستُفشل بناء المُثبِّت
على جهازك في الدقيقة الأولى.

| المؤشر | القيمة |
|---|---|
| الإصدار | 2.9.5 |
| حجم الكود | 120 ملف بايثون، 42,157 سطر |
| المحرك (`engine_v2`) | 29 ملفًا، 8,329 سطر |
| طبقة الوعي (`awareness`) | 11 ملفًا، 8,035 سطر |
| واجهة ويندوز (`windows_app`) | 11 ملفًا، 15,663 سطر |
| الاختبارات | 59 ملفًا، 8,082 سطر |
| الفحص الشامل | **48/48 ناجح، صفر فشل** |
| وحدات تُحزَم في المُثبِّت | 53 وحدة |
| حجم بيانات المُثبِّت | 178.6 م.ب (بعد توفير 168 م.ب) |

---

## ثانيًا: العلّة التي كانت ستُفشل بناء المُثبِّت على جهازك

هذه أهم نتيجة عملية في التقرير، لأنها كانت ستوقفك عند أول محاولة بناء.

ملف مواصفات البناء `AhmedAlFaifiMarketImageStudioV2.spec` كان يطلب
نماذج القص من المسار `resources/models/`، وهو **مجلد غير موجود في
المشروع** — بقية متروكة من بنية الإصدار 1.2.1. والنماذج الثلاثة تسكن
فعليًا في `src/engine_v2/models/`. وPyInstaller يتوقف بخطأ صريح عند
غياب أي ملف بيانات مسرود، فكان البناء ينتهي قبل أن يبدأ.

ولم تقف المشكلة عند المسار. الملف كان يُدرج `u2net.onnx` (168 م.ب)
و`isnet-general-use.onnx` (171 م.ب) معًا، بمجموع **343 م.ب** من
النماذج داخل المُثبِّت. وبمراجعة `runtime_deps_v2.MODEL_FILENAMES`
تبيّن أن المحرك يرتّب النماذج بالأفضلية: `isnet` الأدق، ثم `u2net`
المتوسط، ثم `u2netp` الأخف. أي أن `u2net` منفعته حدية أمام `isnet`،
وآلية التنزيل التلقائي موجودة أصلًا لجلبه عند الحاجة.

**ما أُصلح:**

الأول، تصحيح المسار إلى `engine_v2/models` — وهو بالضبط ما تبحث عنه
الدالة `paths_v2._candidates()` داخل حزمة PyInstaller المجمّدة
(`_MEIPASS`)، فصار الـ`spec` والمحرك يتفقان على مكان واحد.

الثاني، حزم `isnet` (الأدق، إلزامي) مع `u2netp` (4.4 م.ب فقط) كشبكة
أمان خفيفة تضمن عمل القص في كل الأحوال، واستثناء `u2net`. النتيجة:
**178.6 م.ب بدل 347 م.ب — توفير 168 م.ب من حجم المُثبِّت** بلا أي
خسارة في الجودة، لأن النموذج الأدق محزوم والاحتياطي الخفيف محزوم.

الثالث، إضافة حاجز يفحص وجود كل ملف بيانات **قبل** بدء التحليل، ويفشل
برسالة عربية تشرح ما الناقص ومن أين يُجلب. هذا مهم عمليًا لأن ملفات
`.onnx` مستبعدة من git لحجمها، فمن يستنسخ المستودع يجد مجلد نماذج
فارغًا — والرسالة الآن تقول له ذلك بوضوح بدل رسالة PyInstaller الغامضة
بعد دقائق من الانتظار.

---

## ثالثًا: الدرس الأعمق — حين يكون العيب في القاضي لا في المتهم

بدأت الجلسة بمشكلة ظاهرها بسيط: الجرّاح الذاتي يرفض رقعًا يبدو أنها
سليمة. والافتراض الطبيعي أن الرقع خاطئة. لكن التحقق المنهجي كشف عكس
ذلك تمامًا: **بيئة التحقق نفسها كانت معطوبة بثلاث علل مستقلة**، كلها
تُنتج الأثر الخبيث ذاته — رفض رقع سليمة.

| العلّة | الموضع | الأثر |
|---|---|---|
| استبعاد `*.pyc` من نسخ الشجرة المؤقتة | `_stage_ignore` | حزمة `smart_catalog_vision` المُصرَّفة تُنسخ فارغة، فيفشل استيراد كل ملف يعتمد عليها |
| مشاركة `HOME` بين عمليات التحقق المتوازية | `_run_cmd` | تنازع على `~/.config`: عملية تُبطل التنشيط وأخرى تتحقق منه في اللحظة نفسها |
| نسخ ثلاثة مجلدات فقط | `_verify_in_sandbox` | `owner_studio` غائب فيفشل استيراده في الشجرة المؤقتة وينجح في الجذر |

العلّة الثالثة أخطرها مفهوميًا. البوابة الفرقية تقوم على مقارنة عادلة:
تشغيل الاختبارات على الأصل وعلى النسخة المُرقَّعة، وقبول الرقعة إن لم
تُحدث ارتدادًا. لكنها كانت تقارن **رقعةً في بيئة ناقصة** بـ**أصلٍ في
بيئة كاملة**، فتُحمّل الرقعة وزر نقص البيئة. وهذا يُبطل قيمة البوابة
الفرقية من أساسها.

> الأخطر أن التنازع غير حتمي **في الاتجاهين**. كما قد يرفض رقعة سليمة،
> قد يُخفي رقعة فاسدة إن رجّح التنازع كفّتها. فيصير حكم البوابات ضجيجًا
> لا دليلًا — وهذا أسوأ من غياب البوابات، لأن الثقة الزائفة تُمرّر ما
> كان الحذر سيمنعه.

**البرهان المقياسي:** ملف `license_v2.py` — أحرج ملف في البرنامج، إذ
يحمل منطق الترخيص والاشتراكات — كان يُرفض بنتيجة `11 ناجح / 3 فاشل`.
بعد الإصلاحات الثلاثة صار يجتاز **البوابات الثمانية كلها**، بما فيها
`test_license.py` و`test_env_resilience.py` اللذان كانا يفشلان. وقد
تحقّقنا مستقلًا أن الرقعة سليمة: تشغيل `test_license.py` مع الرقعة
الكاملة نجح **ثماني مرات من ثماني** بالتشغيل المتسلسل.

---

## رابعًا: كيف تبني المُثبِّت على جهازك

بناء ملف `.exe` يستلزم ويندوز، وهذا قيد لا يُتجاوز بالبرمجة — لا يمكن
إنتاج المُثبِّت في بيئة لينكس. لذلك جُهِّز لك سكربت يقوم بكل شيء بأمر
واحد، مع فحوص مسبقة تمنع إهدار الوقت.

**المتطلبات:** ويندوز 10 أو أحدث، Python 3.11+، و
[NSIS](https://nsis.sourceforge.io) مضافًا إلى `PATH`.

```batch
cd C:\market-image-studio-v2
pip install -r requirements.txt
build\windows\ابنِ_المثبت.bat
```

يمرّ السكربت بخمس مراحل: التحقق من مجلد العمل (لأن الـ`spec` يقرأ
المسارات من مجلد العمل الحالي، فتشغيله من مكان آخر يُنتج حزمة ناقصة
بلا رسالة خطأ)، ثم التحقق من الأدوات وتثبيت PyInstaller إن غاب، ثم
الفحص المسبق للوحدات وملفات البيانات، ثم بناء التطبيق، ثم بناء
المُثبِّت.

الناتج النهائي:
`dist\installer\AhmedAlFaifiMarketImageStudio-Setup-2.9.5.exe`

وإن غاب NSIS فقط، يكتمل بناء التطبيق ويُنبّهك السكربت، وتجد الحزمة
القابلة للتشغيل في `dist\windows\AhmedAlFaifiMarketImageStudio\`.

---

## خامسًا: حواجز السلامة في خط البناء

المشكلة الجوهرية في تحزيم هذا البرنامج أن **استيرادات طبقة الوعي
محميّة بـ`try/except`** لتبقى المعالجة سليمة إن غابت. وهذا تصميم صحيح
وقت التشغيل، لكنه كارثي وقت البناء: لو نسي الـ`spec` وحدات الوعي،
لبُني مُثبِّت يعمل **بصمت بلا وعي** — لا لوحة، ولا حوار عربي، ولا
تنفيذ لأوامر المالك — ولا رسالة خطأ واحدة. عطل لا يكتشفه أحد إلا
المالك بعد التسليم.

ولذلك لا يُسرد شيء يدويًا. الـ`spec` **يكتشف** الوحدات من القرص
(53 وحدة: 29 محرك، 11 وعي، 11 واجهة)، ويحمل قائمة 22 وحدة حرجة
**يفشل البناء صراحةً** إن غابت واحدة منها.

وقد جرى التحقق من الحاجز بـ**اختبار سلبي** لا بالتمني: أخفينا
`u2netp.onnx` مؤقتًا فرصده الفحص وفشل برمز خروج 1 ورسالة عربية
واضحة، وبعد استعادة الملف عاد لرمز 0. فحص لا يفشل عند وجود عيب هو
طمأنينة زائفة، وهي أسوأ من غياب الفحص.

---

## سادسًا: ما بقي للذكاء

توقفنا عن العمل على محركات الذكاء بطلبك، والأرضية جاهزة تمامًا
للاستئناف. لا يوجد تنفيذ يستمر بعد انتهاء الجلسة — لذلك كُتب ملف
`docs/حالة_التسليم_والاستئناف.md` بتفصيل يكفي لاستئناف العمل بلا
إعادة اكتشاف.

**المسار الأول — تطبيق رقع الاستثناء الصامت.** رُصدت 136 علّة موزّعة:
105 استثناء صامت، 30 ارتداد صامت، ومسار مطلق واحد. وكلها من نوع لا
يؤثر في السلوك الوظيفي — إصلاحها **تحسين رصد لا إصلاح عطل**، ولهذا لم
يكن تأجيلها مُضرًّا. المحاكاة الجافة نظيفة على 23 ملفًا بصفر تعديل
ميت، وبيئة التحقق صحيحة الآن، فالتطبيق الفعلي صار مأمونًا.

**المسار الثاني — الفهم الدلالي العربي.** انتهى البحث إلى قرار معماري
مهم: ثلاث طبقات متدرّجة بلا نموذج عصبي ضخم. جوهر المشكلة الحالية أن
«صغّرها» و«تصغير» تُعالجان ككلمتين مختلفتين، والسبب ليس نقص المفردات
بل **غياب التجذيع**. فالحل: تجذيع خفيف (ISRI-lite)، ثم تشابه n-gram
لفهم صيغ لم تُكتب يدويًا، ثم ذاكرة مستمرة تُرقّي الصيغ المصحّحة —
أي التعلّم من التصحيح. التفاصيل في `docs/بحث_محركات_الذكاء.md`.

---

## سابعًا: محاذير تقنية تُوفّر وقتًا

هذه ملاحظات مكتسبة من التجربة المباشرة، تجنّبك إعادة اكتشافها:

حزمة `smart_catalog_vision` **مُصرَّفة فقط** (`.pyc` بلا مصدر) — كود
مملوك لا يُعدَّل، ويجب حفظه في أي عملية نسخ. ومكتبة `cv2` **إلزامية**
(270 استدعاء)، وحمايتها بـ`= None` كارثة تُحوّل خطأ استيراد واضحًا إلى
مئات انهيارات `NoneType` غامضة. والدالة `_license_dir()` تكتب في
`~/.config`، فأي اختبار يلمس الترخيص يحتاج `HOME` معزولًا وإلا حدث
تنازع. وأخطر ما في الإصلاح الآلي هو **الرقعة الزائفة**: رقعة تجتاز كل
الاختبارات وتكون خاطئة — ولهذا صُمّمت البوابات خمسًا لا واحدة.

**بيئة الاختبار الصحيحة:**

```bash
export PYTHONPATH=src:windows_app
export MIS_HEADLESS=1 MIS_LICENSE_BYPASS=1 QT_QPA_PLATFORM=offscreen
python3 tests/run_all.py
```

---

## ثامنًا: نقاط الأمان في git

| الالتزام | الوصف |
|---|---|
| `da44176` | إصلاح خط بناء ويندوز: مسارات النماذج وحاجز البيانات |
| `0191adc` | إصلاح ثلاث علل في بيئة تحقق الجرّاح |
| `4c3dfcf` | تقوية محرك التحويل: تصنيف تبعيات وبوابة معنى |
| `423d44c` | طبقة الوعي الأولى |

التراجع عند الحاجة: `git reset --hard <الالتزام>` أو
`surgeon.rollback_last()` من داخل البرنامج.

---

*أُعِدّ بواسطة Manus AI*
