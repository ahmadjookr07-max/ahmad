# حزمة انتقال الجلسة — Ahmed Al-Faifi Market Image Studio v2.3.0

> هذا الملف يحتوي على كل ما تحتاجه الجلسة الجديدة لفهم المشروع والمشاكل المتبقية والبدء فورًا.

---

## 1. وصف المشروع

**Ahmed Al-Faifi Market Image Studio** — برنامج سطح مكتب ويندوز (Python 3.12 + PySide6/Qt6) لمعالجة صور المنتجات للمتاجر الإلكترونية. يقوم بـ:
- إزالة خلفيات صور المنتجات بالذكاء الاصطناعي (ONNX models: ISNet, u2net)
- ربط الصور بأرقام الأصناف والباركودات من ملف إكسل
- تسمية الصور حسب سياسات محددة (رقم_وحدة، رقم_حبة_شدة_كرتون)
- تحسين جودة الصور (إضاءة، اقتصاص، طمس تواريخ، إزالة انعكاسات)
- اقتصاص حقائق التغذية بجودة عالية
- تصدير حزمة ZIP نهائية جاهزة للرفع على المتجر

---

## 2. البنية التقنية

| العنصر | التفاصيل |
|--------|-----------|
| اللغة | Python 3.12 |
| الواجهة | PySide6 (Qt6) — QtCore, QtGui, QtWidgets فقط |
| المنصة | Windows 10/11 64-bit (تطبيق سطح مكتب، لا واجهة ويب) |
| المحرك | engine_v2 (Python نقي) + smart_catalog_vision (pyc مجمع — legacy) |
| النماذج | ISNet (171MB) + u2net (168MB) + u2netp (4.4MB) — ONNX |
| الترخيص | dilithium_py + cryptography |
| الباركود | zxingcpp |
| OCR | pytesseract (اختياري) |
| الإصدار | 2.3.0 |
| GitHub | ahmad121232414-collab/market-image-studio-v2 |

---

## 3. بنية الملفات الرئيسية

```
market-image-studio-v2/
├── windows_app/
│   ├── native_app.py          ← الملف الرئيسي للواجهة (5394 سطر)
│   ├── native_app_v2.py       ← نقطة الدخول (يستورد native_app)
│   ├── license_ui.py          ← واجهة الترخيص
│   ├── v2_ui.py               ← واجهة V2 (سياسة التسمية)
│   └── assets/                ← أيقونات وموارد
├── src/
│   ├── engine_v2/
│   │   ├── naming_v2.py       ← محرك التسمية (join_all_units)
│   │   ├── integration_v2.py  ← بناء أسماء الإخراج
│   │   ├── nutrition_v2.py    ← حقائق التغذية
│   │   ├── processor_v2.py    ← معالج الصور
│   │   ├── primary_image_v2.py← تعيين الصورة الرئيسية
│   │   ├── license_v2.py      ← محرك الترخيص
│   │   ├── batch_refine_v2.py ← معالجة دفعية
│   │   ├── paths_v2.py        ← مسارات
│   │   ├── models/            ← isnet-general-use.onnx (171MB)
│   │   └── assets/            ← خطوط عربية
│   └── smart_catalog_vision/  ← محرك قديم (ملفات .pyc مجمعة)
├── resources/models/           ← u2net.onnx + u2netp.onnx
├── build/windows/              ← ملف PyInstaller spec
└── VERSION                     ← 2.3.0
```

---

## 4. ما تم إنجازه (الإصدار 2.3.0 — commit 22372dd)

| # | المتطلب | الحالة | الملفات المعدلة |
|---|---------|--------|-----------------|
| 1 | إصلاح شارة "متبق -1 أيام" | ✅ مكتمل | license_ui.py, license_v2.py |
| 2 | إصلاح تداخل شريط الربط المباشر | ✅ مكتمل | native_app.py (_apply_results_layout) |
| 3 | محرر مدمج (تبويب "تحرير مباشر") | ⚠️ موجود لكن سيئ التصميم | native_app.py |
| 4 | زر "ضم للصنف الأعلى" | ✅ مكتمل | native_app.py |
| 5 | زر "تعيين كصورة رئيسية" | ✅ مكتمل | native_app.py, primary_image_v2.py |
| 6 | سياسة التسمية join_all_units | ✅ مكتمل | naming_v2.py, integration_v2.py, v2_ui.py |
| 7 | حقائق التغذية HQ | ⚠️ الكود موجود لكن يتطلب tesseract خارجي | nutrition_v2.py, processor_v2.py |
| 8 | توحيد ترقيم dash | ✅ مكتمل | naming_v2.py, batch_refine_v2.py |

---

## 5. المشاكل المتبقية (الأولوية القصوى) — من صور المستخدم الأخيرة

### مشكلة 1: تبويب "تحرير مباشر" محشور وغير قابل للاستخدام
**الوصف:** الصورة تظهر صغيرة جدًا في مساحة ضيقة، والأدوات مبتورة ومحشورة في عمود ضيق يمين الصورة. المستخدم لا يستطيع رؤية الصورة بوضوح ولا استخدام الأدوات بشكل مريح.

**المطلوب:**
- الصورة يجب أن تأخذ المساحة الكاملة (كبيرة وواضحة)
- الأدوات تكون أسفل الصورة أو في شريط جانبي واسع ومنظم
- نقل **كل** أدوات المحرر القديم (الذي يفتح من زر "محرر الصور" في الشريط العلوي) إلى هنا
- الأدوات المطلوبة: اقتصاص ذكي/يدوي، تحسين إضاءة، طمس تواريخ، إزالة انعكاسات، وزن المنتج، حقائق التغذية

**الموقع في الكود:** `native_app.py` سطر 2237 — دالة `_build_embedded_editor_tab()`

### مشكلة 2: زر "محرر الصور" القديم لا يزال موجودًا
**الوصف:** في الشريط العلوي يوجد زر "محرر الصور" يفتح نافذة منفصلة (الديالوج القديم الكبير). المستخدم طلب صراحةً إزالته ونقل كل أدواته للتحرير المباشر.

**المطلوب:** إزالة الزر من الشريط العلوي، وإزالة الديالوج المنبثق. كل الأدوات تكون في تبويب "تحرير مباشر" فقط.

**الموقع:** الشريط العلوي يُبنى في `native_app.py` — ابحث عن "محرر الصور" أو "image_editor" أو الديالوج `ProtectedEditDialog`

### مشكلة 3: القائمة اليمنى (الربط) محشورة
**الوصف:** أزرار الربط في الجانب الأيمن (ربط الآن، حقائق التغذية، ضم للصنف الأعلى، ربط بصورة أخرى، عرض الصورة، اقتراح قريب، اعتماد مرجع، ربط بالمرجع) كلها محشورة ومتراكمة في مساحة صغيرة.

**المطلوب:**
- توسيع المساحة اليمنى
- ترتيب الأزرار بشكل أفضل (مجموعات منطقية مع مسافات)
- أو نقل بعضها لأماكن أنسب

**الموقع:** `native_app.py` — ابحث عن `manual_group` أو `_build_review` أو القسم الذي يبني أزرار الربط

### مشكلة 4: ربط الصور بالصنف — لا توجد طريقة سهلة
**الوصف:** عندما تكون هناك صورتان (واحدة بباركود وأخرى بدون) للصنف نفسه، لا توجد طريقة سريعة لربطهما سوى كتابة الرقم يدويًا. المستخدم يريد: اختيار صورة من القائمة → ضغط زر → تُربط تلقائيًا بالصنف المحدد أعلاه.

**المطلوب:**
- عند تحديد صورة غير مرتبطة في القائمة، يظهر زر واضح "ربط بالصنف المحدد أعلاه" أو "ربط بالصنف [رقم]"
- أو: اختيار عدة صور ثم ضغط زر واحد لربطها كلها بنفس الصنف
- الزر الحالي "ضم للصنف الأعلى" موجود لكن يبدو أنه لا يعمل بالشكل المطلوب أو غير واضح

### مشكلة 5: حقائق التغذية تتطلب tesseract خارجي
**الوصف:** خاصية اقتصاص حقائق التغذية تعتمد على pytesseract الذي يتطلب تثبيت Tesseract OCR من مصدر خارجي. المستخدم يريد أن تعمل بدون أي تثبيت إضافي.

**المطلوب:**
- إما تضمين Tesseract في الحزمة المحمولة
- أو (الأفضل): جعل اقتصاص حقائق التغذية يعمل بدون OCR — المستخدم يحدد المنطقة يدويًا (اقتصاص حر) والبرنامج يقتصها بجودة عالية ويحفظها كصورة منفردة
- الاقتصاص يكون من الصورة الأصلية بالدقة الكاملة (لا تصغير)
- تكبير ذكي إذا كانت المنطقة صغيرة

---

## 6. المحرر القديم (النافذة المنفصلة) — أدواته التي يجب نقلها

من الصورة الثالثة التي أرسلها المستخدم، المحرر القديم يحتوي:

**الأدوات الذكية (تلقائية):**
- معالجة ذكية كاملة (زر واحد)
- إزالة الخلفية (قص ذكي)
- تحسين تلقائي
- توسيط وتأطير 800×700
- الظل (أسفل المنتج) مع شريط قوة
- إزالة انعكاسات التصوير (اللمعان) مع شريط قوة
- المساعد الذكي — اقتراحات
- تنعيم الحواف الذكي
- إزالة هالة الخلفية
- منطقة العزل (اللمعان الانتقائي)

**الأدوات اليدوية (دقيقة):**
- قلم التبييض وضبط الحواف (فلم تبييض / استرجاع / تحريك)
- طمس تاريخ يدوي / طمس التواريخ تلقائيًا
- حجم الفرشاة (شريط)
- نعومة الحواف (شريط)
- منزلقات التحسين: الإضاءة، التباين، الحدة، التشبع، إزالة الضوضاء
- تصغير المنزلقات (زر)
- اقتصاص وتوريس المنتج (دوران دقيق): منزلق + أزرار (تصغير، -0.5°، +0.5°، توريس تلقائي ذكي)
- اقتصاص للتحديد
- تفعيل استوديو للتسليم (حواف نظيفة + لمعة متجر)

**أوضاع التشغيل:**
- الوضع الذكي
- الوضع اليدوي
- وضع الدمج (ذكي + يدوي بالمناطق)

**شريط علوي:**
- فتح صورة للبدء
- حفظ WebP / حفظ
- تراجع / إعادة
- قبل / بعد
- إعادة ضبط الكل
- ملاءمة
- تعليمات
- نسبة التكبير 100%

---

## 7. تصميم الواجهة المطلوب (رؤية المستخدم)

**استوديو المراجعة والربط** يجب أن يكون:

```
┌─────────────────────────────────────────────────────────────────┐
│ [شريط علوي: حفظ الجلسة | أداة إعادة التسمية | تعليمات | المالك] │
├─────────────────────────────────────────────────────────────────┤
│ [إحصائيات: إجمالي | مطابق | مراجعة | أخطاء]                    │
├────────────────────────────────┬────────────────────────────────┤
│                                │  قائمة الصور والصنف            │
│   [بطاقة الصنف المحدد]        │  (جدول: صورة/حالة، صنف/باركود │
│   اسم + باركود + ملف          │   اسم الصنف)                   │
│                                │                                │
│   ┌────────────────────────┐   │  ─────────────────────────────  │
│   │                        │   │  الربط المباشر                 │
│   │   الصورة كبيرة جدًا    │   │  [حقل: اسم/رقم/باركود]        │
│   │   (80% من المساحة)     │   │  [ربط الآن]                    │
│   │                        │   │  [ضم للصنف الأعلى]             │
│   │                        │   │  [حقائق التغذية]               │
│   └────────────────────────┘   │  [عرض الصورة]                  │
│                                │  [ربط بصورة أخرى]              │
│   [شريط أدوات التحرير أسفل    │                                │
│    الصورة — مضغوط ومنظم]      │                                │
│   اقتصاص|تحسين|طمس|انعكاسات  │                                │
│   |وزن|تغذية|حفظ|تراجع        │                                │
│                                │                                │
├────────────────────────────────┴────────────────────────────────┤
│ [حفظ حزمة ZIP] [فتح مجلد النتائج]                              │
└─────────────────────────────────────────────────────────────────┘
```

**النقاط الجوهرية:**
1. الصورة تأخذ أكبر مساحة ممكنة — واضحة وكبيرة
2. أدوات التحرير كشريط أيقونات أسفل الصورة (مثل Photoshop) — لا في عمود جانبي ضيق
3. كل أداة عند الضغط عليها تفتح لوحة صغيرة أسفلها (أو popup صغير) بالخيارات
4. القائمة اليمنى مرتبة ومريحة — أزرار الربط واضحة ومتباعدة
5. لا نوافذ منبثقة كبيرة — كل شيء في مكانه

---

## 8. تعليمات للجلسة الجديدة

### كيف تبدأ:
1. استنسخ المستودع: `gh repo clone ahmad121232414-collab/market-image-studio-v2`
2. الملف الرئيسي: `windows_app/native_app.py` (5394 سطر)
3. اقرأ هذا الملف (SESSION_HANDOFF.md) كاملًا أولًا
4. ركّز على المشاكل الخمس في القسم 5 أعلاه

### أولويات العمل:
1. **أولًا:** إعادة تصميم تبويب "تحرير مباشر" بالكامل — الصورة كبيرة + أدوات كشريط أسفلها
2. **ثانيًا:** نقل كل أدوات المحرر القديم (القسم 6) إلى التحرير المباشر
3. **ثالثًا:** إزالة زر "محرر الصور" من الشريط العلوي وإزالة الديالوج المنبثق
4. **رابعًا:** إصلاح القائمة اليمنى (الربط) — مساحة أوسع وترتيب أفضل
5. **خامسًا:** حقائق التغذية — اقتصاص يدوي بجودة عالية بدون tesseract
6. **سادسًا:** بناء الحزمة المحمولة وتسليمها

### قواعد مهمة:
- هذا تطبيق ويندوز سطح مكتب — **لا موقع ويب**
- لا تستخدم GitHub Actions — بناء الحزمة محليًا
- الحزمة المحمولة: Python 3.12 embeddable + wheels ويندوز + كود + نماذج AI
- اختبار بـ Wine على Linux (numpy/cv2 تعلق في Wine لكن تعمل على ويندوز حقيقي)

### ملفات مرجعية مهمة:
- `build/windows/AhmedAlFaifiMarketImageStudioV2.spec` — ملف PyInstaller الرسمي (مرجع للبنية)
- `windows_app/native_app.py` سطر 2237 — المحرر المدمج الحالي
- `windows_app/native_app.py` سطر 2340 — لوحة أدوات المحرر
- `windows_app/native_app.py` سطر 1153 — ImagePreviewPane
- `windows_app/native_app.py` سطر 567 — ZoomableImageView

---

## 9. الحزمة المحمولة (للمرجع)

الحزمة المحمولة بُنيت سابقًا في `/home/ubuntu/portable_build/` وتم تسليمها كـ ZIP (484MB). بنيتها:

```
AhmedAlFaifiMarketImageStudio/
├── AhmedAlFaifiMarketImageStudio.bat   ← ملف التشغيل
├── اقرأني - طريقة التشغيل.txt
├── python/                              ← Python 3.12 embeddable
│   ├── python.exe
│   ├── python312.dll
│   ├── python312._pth                   ← مسارات مضبوطة
│   └── Lib/site-packages/              ← PySide6 (مقلص 165MB) + numpy + PIL + ...
└── app/                                 ← كود البرنامج
    ├── windows_app/
    ├── src/engine_v2/
    ├── src/smart_catalog_vision/        ← ملفات .pyc
    ├── resources/models/
    └── VERSION
```

---

## 10. ملاحظات إضافية من المستخدم (عبر كل الجلسات)

- المستخدم يعمل في مجال Growth
- يريد البرنامج بسيط وسهل الاستخدام — لا تعقيد
- كل الخيارات مباشرة على الأصناف (ما عدا "ضبط الصور القديمة" و"سياسة التسمية")
- الصورة الرئيسية بدون رقم، الإضافية -1/-2/-3
- الوحدات من الإكسل حرفيًا (حبة، شدة، كرتون)
- حقائق التغذية تُقتص بجودة عالية جدًا وتُحفظ كصورة منفردة ضمن صور الصنف
- طريقة الربط: صورة بباركود (تُربط تلقائيًا) + صورة بدون باركود (يجب ربطها يدويًا بسرعة)
