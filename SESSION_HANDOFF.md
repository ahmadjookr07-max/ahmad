# حزمة انتقال الجلسة — استوديو صور المتجر

**الإصدار 2.9.5** | آخر تحديث: 2 أغسطس 2026 | آخر كوميت: `f2ec958`
**المستودع:** `ahmad121232414-collab/market-image-studio-v2` (خاص)

> هذه الوثيقة مكتوبة لتكون **المصدر الوحيد** الذي تحتاجه جلسة جديدة
> لفهم المشروع والبدء فورًا. النسخة السابقة (2.3.0) كانت متقادمة وتصف
> مشاكل حُلَّت جميعها؛ هذه النسخة تعكس الحالة الفعلية للكود.

---

## 1. ما هو البرنامج

**استوديو صور المتجر** برنامج سطح مكتب لويندوز (Python + PySide6/Qt6)
يحوّل صور المنتجات الخام إلى حزمة صور جاهزة للرفع على المتجر
الإلكتروني، ويربط كل صورة برقم صنفها من ملف الإكسل.

المسار الذي يسلكه المستخدم يبدأ من مجلد صور خام وينتهي بحزمة ZIP
مرتّبة: يقرأ البرنامج الباركود من الصورة ويربطها بالصنف تلقائيًا، ثم
يعرض ما لم يُربط في استوديو مراجعة يُتمّ الربط فيه يدويًا بضغطة، ثم
يزيل الخلفية ويحسّن الإضاءة ويقصّ ويضبط الميول، ويسمّي الملفات حسب
سياسة الوحدات المقروءة حرفيًا من الإكسل، ويحفظ WebP نهائيًا.

يعمل البرنامج باشتراك يُصدره المالك من **برنامج منفصل** (استوديو
المالك) بمفتاح مربوط ببصمة جهاز العميل.

---

## 2. البنية التقنية

| العنصر | التفاصيل |
|---|---|
| اللغة | Python 3.11+ (مُختبر على 3.11 و3.12) |
| الواجهة | PySide6 (Qt6) — QtCore, QtGui, QtWidgets |
| المنصة | Windows 10/11 64-bit — تطبيق سطح مكتب، **لا موقع ويب** |
| المحرك | `src/engine_v2/` بايثون نقي |
| عزل الخلفية | ONNX (ISNet + u2net) عبر `onnxruntime` |
| الترخيص | توقيع هجين: RSA (`cryptography`) + PQC (`dilithium-py`) |
| قراءة النصوص | `pytesseract` (اختياري — لجدول القيم الغذائية والتواريخ) |
| حجم الكود | 86 ملف بايثون، ~21,800 سطر |
| الإصدار | 2.9.5 |

---

## 3. خريطة الملفات

```
market-image-studio-v2/
├── windows_app/                    ← واجهة برنامج المستخدم
│   ├── native_app.py      7,635 سطر ← النافذة الرئيسية وكل المنطق
│   ├── v2_ui.py           1,697 سطر ← ترقيع v2 (شريط أدوات، سياسة التسمية)
│   ├── native_app_v2.py     542 سطر ← نقطة الإطلاق (تُشغِّل الترقيع ثم النافذة)
│   ├── photo_editor_v2.py           ← نافذة المحرر الاحترافي المتقدم
│   ├── unified_editor.py            ← المحرر المدمج في تبويب «تحرير»
│   ├── license_ui.py                ← نوافذ التنشيط والشارة
│   ├── ui_scale.py                  ← نظام المقياس التكيفي (لا أرقام صلبة)
│   └── nutrition_crop.py            ← اقتصاص جدول القيم الغذائية
├── owner_studio/
│   └── owner_studio.py    1,066 سطر ← برنامج المالك: إصدار الاشتراكات
├── src/engine_v2/                   ← المحرك
│   ├── naming_v2.py                 ← التسمية وسياسة الوحدات
│   ├── integration_v2.py            ← بناء أسماء الإخراج
│   ├── processor_v2.py              ← معالجة الصورة (عزل، تحسين، تأطير)
│   ├── segmentation_v2.py           ← عزل الخلفية ONNX
│   ├── batch_refine_v2.py           ← ضبط الصور القديمة دفعيًا
│   ├── nutrition_v2.py              ← جدول القيم الغذائية
│   ├── primary_image_v2.py          ← تعيين صورة الواجهة (★)
│   ├── license_v2.py                ← محرك الترخيص والتحقق
│   └── models/                      ← نماذج ONNX
├── tests/                    50 ملف ← اختبارات
├── docs/                            ← وثائق الحالة
├── requirements.txt                 ← التبعيات (مُحدَّث 2.9.5)
└── VERSION                          ← 2.9.5
```

**نقطة الإطلاق:** `python windows_app/native_app_v2.py`
**برنامج المالك:** `python owner_studio/owner_studio.py`

---

## 4. قاعدة الحكم على أي تعديل: «لا تكرار»

هذه أهم قاعدة في المشروع، وقد سبق أن كُسرت مرتين فأُصلحت في 2.9.5.

> **كل وظيفة تسكن مكانًا واحدًا فقط. عند نقل وظيفة يُحذف الأصل من
> الجذور — لا يُخفى ولا يُعطَّل ولا يُترك «للاحتياط».**

سبب الصرامة أن الترقيع في هذا المشروع يمرّ عبر طبقتين: `native_app.py`
يبني الواجهة، ثم `v2_ui.py` و`native_app_v2.py` يرقّعانها ويضيفان
أزرارًا في وقت التشغيل. إخفاء زر في الطبقة الأولى **لا يمنع** الطبقة
الثانية من إعادة إضافته — وهذا بالضبط ما حدث: أُلغي زر أداة التسمية من
`native_app.py` لكن `native_app_v2.py` ظل يضيفه، فبقيت الوظيفة في
مكانين والمستخدم رآها.

**لذلك عند حذف أي منفذ يجب تفتيش الطبقات الثلاث معًا:**

```bash
grep -rn "اسم_الزر\|objectName\|دالة_الفتح" \
  windows_app/native_app.py windows_app/native_app_v2.py windows_app/v2_ui.py
```

ويجب حذف أربعة أشياء لا واحد: **الزر**، **دالة الفتح**، **الصنف/النافذة
نفسها**، **قواعد CSS الخاصة به**.

---

## 5. ما حُذف فعلًا في 2.9.5 (لا تُعِده)

| ما حُذف | كان في | البديل الوحيد الآن |
|---|---|---|
| نافذة `BulkRenameDialog` (508 سطر) | `v2_ui.py` | زر **«فتح مجلد منجز»** داخل استوديو المراجعة |
| دالة `open_rename_tool` و`v2_open_rename_tool` | `v2_ui.py` | — |
| دالة `_open_bulk_rename` | `native_app.py` | — |
| زر «أداة إعادة التسمية» في شريط الأدوات | `native_app_v2.py` | — |
| أنماط CSS لـ `v2RenameBtn` و`v2EditorBtn` | `v2_ui.py` | — |

صافي الحذف: **604 سطر مُزال** مقابل 122 مُضاف (كوميت `f2ec958`).

**زر `v2EditorBtn`** (محرر الصور كنافذة منفصلة في الشريط) حُذف سابقًا
في 2.6 لأن أدواته دُمجت في تبويب **«✎ تحرير»** داخل النافذة. أنماطه
الميتة بقيت حتى نُظّفت في 2.9.5.

هناك اختبارات **تحرس** عدم عودة هذه الوظائف؛ إن أعدتها ستفشل:
`no_rename_duplicate`، `rename_dialog_removed`، `rename_port_removed`،
`no_visible_rename_button`، و`assert "v2EditorBtn" not in names`.

---

## 6. قواعد وظيفية غير قابلة للتفاوض (قرارات المالك)

**التسمية.** الوحدة تُقرأ من الإكسل **حرفيًا** ولا يُصحَّح إملاؤها في
اسم الملف: إن كُتبت «حبه» تبقى «حبه»، و«حبة» تبقى «حبة». التطبيع
يُستخدم **للمقارنة الداخلية فقط** (`unit_key`/`same_unit`) لا للاسم
الظاهر. صورة الواجهة بلا رقم، والإضافية `-1` ثم `-2` ثم `-3`.

**الإكسل مرجع مطلق.** المطابقة يجب أن تكون 100%، وحتى الصنف ذو الصورة
الواحدة تُقرأ وحدته من الإكسل. الصنف الغائب عن الإكسل يبقى بلا تغيير.

**الواجهة.** المهم مباشرة على الشاشة، والمتقدم في نافذة تُفتح للتعديل
الفوري. النصوص المزدحمة تُستبدل برموز مع تلميح عند المرور بالفأرة. لا
أرقام أبعاد صلبة — كل مقاس يمرّ عبر `ui_scale` ليتكيّف حتى 800×600 بلا
قصّ نص ولا شريط تمرير أفقي.

**عند تعديل رقم صنف يدويًا** يبقى المستخدم في موضعه ولا يُرجَع للبداية.

**اسم المنتج** يُعرض كاملًا في الربط اليدوي مهما طال.

**الأداء.** تحسين السرعة مطلب دائم، والبرنامج يعمل تلقائيًا بأقل تفاعل.

---

## 7. حالة الاختبارات (بعد 2.9.5)

### ناجحة بالكامل — هذه هي التي تحمي البرنامج

| الاختبار | النتيجة | يغطي |
|---|---|---|
| `test_full_ui_smoke.py` | 26 / 26 | إقلاع الواجهة، كل النوافذ، حراسة عدم التكرار |
| `test_gui_smoke_v21.py` | 8 / 8 | الأزرار، المحرر، الدفعات |
| `test_owner_studio.py` | 26 / 26 | إصدار وتجديد وإلغاء الاشتراك، TOTP |
| `test_owner_link_v21.py` | 10 / 10 | ربط المالك بالمستخدم ورفض التلاعب |
| `test_license_ui.py` | 14 / 14 | التنشيط والشارة والإلغاء |
| `test_license_pqc.py` | 6 / 6 | التوقيع المقاوم للكم |
| `test_responsive_audit.py` | 20 / 20 | التكيف مع الشاشات الصغيرة |
| `test_legacy_folder_unified.py` | كل الفحوص | المجلدات المنجزة والتسمية |
| `diag_naming.py` | 0 فشل | سياسة التسمية والوحدات |

### فاشلة — دين تقني قديم سابق لهذه الجلسة، لا يمس وظيفة البرنامج

| السبب | الملفات |
|---|---|
| تبعية غير مركبة في الساندبوكس (`onnxruntime`) | `test_engine_rebuild`, `test_shadow_v2` |
| تبعية غير مركبة (`pytesseract`) | `test_nutrition_smart_v2`, `test_quality_date_v21` |
| مسارات مشروع قديم محذوف (`/home/ubuntu/v2_project/`) | `test_baseline`, `test_naming_catalog`, `test_shadow_single` |
| توقيع دالة تطوّر والاختبار لم يُحدَّث | `test_processor_v2`, `test_rebuild_mode`, `test_session_webp`, `test_seg_v2`, `test_units`, `test_integration`, `test_nutrition_v2`, `test_batch_refine` |
| مفتاح موقَّع بمفتاح غير مفتاح المالك ⇒ الرفض **سلوك صحيح** | `test_license` |
| بطء الساندبوكس (0.533s مقابل حد 0.5s) | `test_perf_v21` |

**لا يوجد أي فشل سببه حذف التكرار.** إن أردت تصفير القائمة فالمسار هو
تحديث توقيعات الاختبارات القديمة وتركيب `onnxruntime` و`pytesseract`.

---

## 8. برنامج الاشتراك (استوديو المالك)

برنامج مستقل في `owner_studio/owner_studio.py` يصدر المفاتيح ويديرها.
المدد المدعومة كلها مُختبرة ويُعرض نصها للمستخدم بالعربية: أسبوعي
(متبق 6 أيام)، شهري (29)، سنوي (364)، ودائم («الترخيص دائم وفعال»).

الأمان مُختبر ضد أربع هجمات وكلها مرفوضة: تعديل مدة الاشتراك في
المفتاح، استخدامه على جهاز آخر (بصمة الجهاز)، حذف التوقيع المقاوم
للكم، وتصنيع مفتاح من مهاجم. التوقيع هجين RSA + Dilithium بطول 4647
حرفًا، والإلغاء عن بُعد يعمل. البرنامج يوفر كذلك نسخة احتياطية للشفرة
والعملاء، ورسالة عميل جاهزة تحوي المفتاح والتعليمات، ومصادقة ثنائية
TOTP مع رابط `otpauth` وبديل نسخ السر يدويًا.

**الحكم: جاهز وسليم ولا يحتاج تدخلًا.**

---

## 9. كيف تبدأ جلسة جديدة

```bash
gh repo clone ahmad121232414-collab/market-image-studio-v2
cd market-image-studio-v2
pip install -r requirements.txt
sudo apt install python3-tk            # لاستوديو المالك على لينكس

# التشغيل
python windows_app/native_app_v2.py    # برنامج المستخدم
python owner_studio/owner_studio.py    # برنامج المالك

# الاختبار (على لينكس بلا شاشة)
export PYTHONPATH=$PWD/src:$PWD/windows_app:$PWD/owner_studio
export QT_QPA_PLATFORM=offscreen
python tests/test_full_ui_smoke.py
python tests/test_gui_smoke_v21.py
```

**قبل أي تعديل:** اقرأ القسم 4 (قاعدة لا تكرار) والقسم 6 (القواعد غير
القابلة للتفاوض). **بعد أي تعديل:** شغّل `test_full_ui_smoke.py`
و`test_gui_smoke_v21.py` — هما شبكة الأمان.

**عند التسليم:** المالك يريد **كود المصدر كاملًا** (كل ملفات `.py`
والإعدادات و`requirements.txt`) لا ملف تثبيت مُجمَّع، وأي إصلاح
يُعرض عليه للمراجعة قبل اعتباره نهائيًا.

---

## 10. المتبقي المعروف

**قراءة جدول القيم الغذائية** تعتمد على محرك Tesseract الخارجي. المسار
المفضّل هو الاقتصاص اليدوي بجودة عالية من الصورة الأصلية بالدقة
الكاملة (بلا تصغير، مع تكبير ذكي للمناطق الصغيرة) بحيث تعمل الخاصية
دون أي تثبيت إضافي.

**أدوات مطلوبة سابقًا** ويُستحسن التحقق من اكتمالها: إزالة انعكاسات
التصوير، والتكبير والتنقل بالفأرة داخل المحرر بسلاسة، وحذف/تعديل الصور
بمرونة حسب الرقم أو الاسم أو نوع الوحدة.

**تصفير الاختبارات المتقادمة** (القسم 7) متى توفّر وقت.
