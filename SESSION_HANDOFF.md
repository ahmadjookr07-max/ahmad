# نقل الجلسة — استوديو صور المتجر 2.9.9

> **اقرأ هذا الملف كاملًا قبل أي تعديل.** يحوي كل ما تغيّر وكل ما أُزيل
> وكل ما بقي. المشروع في `/home/ubuntu/work` — استنساخ git مربوط
> بالمستودع على الفرع `main`.

## 1. البطاقة السريعة

| البند | القيمة |
| --- | --- |
| النسخة | **2.9.9** (`VERSION`، `native_app.py:214`، `native_app_v2.py:20`) |
| المنصة | ويندوز سطح مكتب، Python 3.11 + PySide6 (Qt6)، **لا واجهة ويب** |
| نقطة الإطلاق | `python windows_app/native_app_v2.py` |
| الواجهة | `windows_app/native_app.py` (8,496 سطرًا) |
| المحرك | `src/engine_v2/` |
| برنامج المالك | `python owner_studio/owner_studio.py` |
| المُثبِّت | `build/windows/installer.nsi` (المستخدم) · `installer_owner.nsi` (المالك) |
| الاختبارات | **63 ملفًا — 63 ناجح / 0 فاشل / 0 محجوب** |
| المستودع | `ahmad121232414-collab/market-image-studio-v2` · المجلد **مربوط** بـgit (`origin`, `main`) |

**البرنامج:** يحوّل صور المنتجات الخام إلى حزمة جاهزة للمتجر، يقرأ
الباركود ويربط الصورة بصنفها من الإكسل، يعرض غير المربوط في استوديو
مراجعة، يعزل الخلفية ويحسّن ويقصّ ويضبط الميول، يسمّي حسب وحدات الإكسل،
ويحفظ WebP. يعمل باشتراك يُصدره المالك بمفتاح مربوط ببصمة الجهاز.


## عائق مفتوح: ورشة البناء تحتاج رفعًا يدويًا

**الحالة:** ورشة `.github/workflows/build-windows.yml` في المستودع البعيد
**ما زالت تبني الإصدار 2.0.0** لتطبيق 2.9.9، وتستدعي `installer_v200.nsi`
الذي حُذف في هذا الإصدار. **فأي بناء آلي الآن سيفشل** بـ«Installer missing».

**السبب:** رمز GitHub المستخدم في الجلسة لا يملك صلاحية `workflows`، فرُفض
دفع الملف عبر git وعبر REST API كليهما (403). القيد على الرمز لا على العمل.

**النسخة المصلحة جاهزة:** `build/ci/build-windows.yml.جاهز-للرفع`
تقرأ الإصدار من `VERSION` وقت التشغيل، وتبني المثبتين (المستخدم والمالك)،
وتولّد بيانات الإصدار قبل PyInstaller، وتحسب SHA256.

**خطوات الرفع (دقيقة واحدة):**
1. افتح المستودع على GitHub ⇒ `.github/workflows/build-windows.yml` ⇒ تعديل
2. امسح المحتوى بالكامل والصق محتوى `build/ci/build-windows.yml.جاهز-للرفع`
3. احفظ (Commit changes)
4. احذف `.github/workflows/build-owner-studio.yml` — متقادمة (تبني 1.0.0)
   والورشة الجديدة تبني المالك أصلًا

**البديل:** امنح الرمز صلاحية `workflows` ويُدفع آليًا.

**حتى يُرفع:** البناء المحلي يعمل بلا عائق عبر
`build/windows/ابنِ_المثبت.bat` (مُحدَّث ويشير إلى `installer.nsi`).


## 2. تشغيل الاختبارات

**الحزمة كاملة — وهي المعتمدة:**
```bash
cd /home/ubuntu/work
export QT_QPA_PLATFORM=offscreen MIS_HEADLESS=1 MIS_LICENSE_BYPASS=1
python3 tests/run_all.py        # ~12 دقيقة
```
`run_all.py` يجمع الملفات بـ`glob("test_*.py")`، فأي اختبار جديد
**يُدرَج تلقائيًا** بلا تسجيل يدوي.

**اختبارات مفردة للتشخيص السريع:**
```bash
python3 tests/test_owner_rules_all.py        # قواعد المالك
python3 tests/test_all_buttons.py            # 44 ضغطة، 0 أخطاء
python3 tests/test_version_consistency.py    # تناسق الإصدار (28 فحصًا)
python3 tests/verify_version_guard.py        # تحقق سلبي: أيكشف الحاجز العطب؟
```

**تهيئة صندوق جديد** — بلاها تُحجب اختبارات لأسباب بيئية لا برمجية:
```bash
sudo pip3 install opencv-python-headless onnxruntime pytest pyflakes \
                  pyinstaller pyyaml pefile
sudo apt-get install -y python3-tk nsis tesseract-ocr tesseract-ocr-ara
```
وتحتاج اختبارات بيانات المالك الحقيقية مجلد `/home/ubuntu/owner_data`.

---

## 3. التعديل الأول — قاعدة الترقيم (المشكلة 1) · **مكتمل**

**قرار المالك:** الرئيسية **بلا رقم**، والإضافية تحمل **ترتيبها الحقيقي**
`-2`، `-3`، `-4`…

**العلة القديمة:** الصيغة كانت `seq - 1`، فتُعطي الإضافية الأولى `-1`،
فيخرج في مجلد واحد `10001102_حبه` و`10001102_حبه-1` وكأنهما صورتان
للترتيب الأول — تداخل يُربك المالك والمتاجر.

**سبعة مواضع تُنتج الأسماء — كلها مُصلَحة:**

| # | الموضع | الدالة | النتيجة |
| --- | --- | --- | --- |
| 1 | `naming_v2.py` | `build_name_join_all` | `900_حبه_باكت`، `-2`، `-3` |
| 2 | `naming_v2.py` | `build_name_dash` | `900_حبه`، `-2`، `-3` |
| 3 | `naming_v2.py:479` | `_fmt_seq` | القالب المخصص `-2`، `-3` |
| 4 | `naming_v2.py:541` | `render` | الرئيسية بلا رقم |
| 5 | `legacy_folder_v2.py:322` | `_target_stems` | المجلد المنجز |
| 6 | `primary_image_v2.py` | `_stem_names` | إعادة الترقيم بالنجمة |
| 7 | `batch_refine_v2.py:218` | مسار الطوارئ | متوافق |

> **الموضع 4 كان مفقودًا حتى نهاية الجلسة.** الشرط كان `total <= 1` فقط،
> فصنف له ثلاث صور يُخرج `900_حبه-1` للرئيسية في **القالب المخصص** بينما
> نمط الشرطة يُخرجها بلا رقم — فيخرج المجلد بصيغتين. صار الشرط `seq <= 1`.

**التحقق:**

```
join_all : ['900_حبه_باكت', '900_حبه_باكت-2', '900_حبه_باكت-3']
dash     : ['900_حبه',      '900_حبه-2',      '900_حبه-3']
legacy   : ['900_حبه_باكت', '900_حبه_باكت-2', '900_حبه_باكت-3']
primary  : ['900_حبه',      '900_حبه-2',      '900_حبه-3']
custom   : ['900_حبه',      '900_حبه-2',      '900_حبه-3']
```

`seq_start` و`seq_pad` و`always_number_single` محترمة: الترقيم القسري مع
`seq_pad=2` يُخرج `900_حبه-01`.

**قاعدة الوحدة ثابتة:** تُقرأ من الإكسل **حرفيًا** ولا يُصحَّح إملاؤها في
الاسم («حبه» تبقى «حبه»). التطبيع للمقارنة الداخلية فقط.

---

## 4. التعديل الثاني — إزالة نسبة التشابه · **مكتمل**

**قرار المالك:** ألغِ نسبة التشابه بالكامل؛ رقم غير موثوق ولا مفهوم.

**المحذوف نهائيًا من `native_app.py`:**

```
VisualSignatureWorker      _queue_visual_signatures
_visual_suggestion_for     _flush_visual_signature_queue
_visual_sig_lookup         _on_visual_signatures_ready
_warm_visual_signatures    _visual_sig_cached
_visual_sig_lru / _visual_sig_pending / _visual_warm_timer
```

ومعها نصوص الواجهة: «نسبة التشابه»، «تشابه بصري»، «التشابه البصري».

**البديل** في `_refresh_smart_link_button` (سطر 6617): أقرب صورة مرتبطة
أعلى القائمة — ترتيب التصوير الطبيعي. دليل واحد واضح، والقرار يبقى
يدويًا: الزر يقترح ولا يربط إلا بضغطة.

**مكسب جانبي:** كانت المنظومة تقرأ كل صورة من القرص وتفكّ ترميزها
(~47 مللي ثانية للصورة) على خيط الواجهة، فتُجمّد النافذة خمس ثوانٍ على
دفعة من 109 صنف. الآن **لا قراءة قرص** في مسار تحديث الاختيار.

**الحراسة:** حُذف `tests/test_visual_sig_async.py` (كان يحرس وجود
المنظومة) واستُبدل بـ`tests/test_visual_sig_removed.py` — 30 فحصًا
تُفشل البناء إن عاد أي اسم محذوف. الفحص يتجاهل التعليقات ونصوص التوثيق
لأن الشفرة تحتفظ بتعليقات تشرح سبب الحذف وتذكر الأسماء صراحةً.

---

## 5. التعديل الثالث — النجمة تُخفي الصورة (المشكلة 2) · **مكتمل**

**البلاغ:** عند الضغط على النجمة لتعيين صورة رئيسية تختفي الصورة من
الجدول أو يظهر «ملف الإخراج غير موجود».

**السبب الجذري (مركّب — أُثبت بالاختبار العملي لا بالقراءة):**

1. `_stem_names` في `primary_image_v2.py` كانت على الترقيم القديم `-1`،
   فتُنتج أسماء تتصادم مع أسماء نمط الشرطة الجديد في المجلد نفسه.
2. الجدول يُفهرس بـ`source_name`، والنجمة **تُغيّر** الاسم على القرص؛
   فيُلتقط الموضع بالاسم القديم بعد التغيير ⇒ لا يُوجد الصف ⇒ يُخفى أو
   يُلغى تحديده ⇒ **الصورة تختفي**.
3. غياب ملف واحد من المجموعة كان يُجهض العملية كلها بلا استرجاع.

**الإصلاحات في `_set_primary_image`:**

- تطبيع المسارات قبل المقارنة (دالة مضافة قبل `_set_primary_image`).
- استرجاع ذكي للملف المفقود: بحث بالجذع في المجلد بدل الإجهاض.
- **ترجمة الموضع**: يُلتقط موضع الصف قبل التغيير ويُترجم للاسم الجديد
  بعده، فلا يفقد الجدول الصف.
- تحمّل غياب ملف: تُعاد تسمية الموجود ويُسجّل الناقص.

---

## 6. الاختبارات المحدَّثة

كانت توقّع الترقيم القديم فحُدّثت: `test_primary_image` ·
`test_legacy_folder_unified` · `test_batch_naming_patch` ·
`test_batch_refine` · `test_naming_join_all` · `test_output_stem_policies` ·
`test_units` (`plan_suffix_dash`) · `test_owner_rules_all` (القالب المخصص) ·
`test_gui_smoke_v21` (كان يرى «التشابه» في التعليقات فيفشل ظلمًا).

**`test_all_buttons.py`:** كان ينهار بـ`SIGABRT` («QThread: Destroyed
while thread is still running») **بعد** طباعة نتائجه الناجحة فيُقرأ
فاشلًا. السبب هدم النافذة وخيط خلفي يعمل. الحل: `win._shutdown_workers(2000)`
قبل `win.close()`. النتيجة: 44 ضغطة، 0 أخطاء، 0 بطيء، 0 حاجب، تغطية 61%.

**المُثبِّت:** وُحِّدت ثماني نسخ مكررة في `installer.nsi` واحد يقرأ
الإصدار من `VERSION` عبر `!searchparse` مع `${__FILEDIR__}` (مُتحقَّق
بـmakensis: أنتج `...Setup-2.9.9.exe`). ومُثبِّت المالك
`installer_owner.nsi` يبقى مستقلًا لأنه برنامج آخر (admin/HKLM/أسرار)
لكنه يقرأ الإصدار من `VERSION` أيضًا. فنجح `test_package_integrity`
(23/23) وصار يحرس «لا تكرار» و«لا إصدار مثبّت نصيًا».

---

## 7. متطلبات بيئية للاختبارات — **لا إخفاقات**

الحزمة اليوم **63/63 · صفر فاشل · صفر محجوب**. لكن اثني عشر اختبارًا
تحتاج بيانات أو حزمًا خارجية، وكانت تُحجب قبل تهيئة الصندوق. الجدول
يبقى مرجعًا لتهيئة أي صندوق جديد:

| الاختبار | ما يحتاجه |
| --- | --- |
| `test_full_run`، `test_real_batch`، `test_match_speed`، `test_link_units_new_side` | `/home/ubuntu/owner_data` (بيانات المالك) |
| `test_legacy_real_owner`، `test_owner_units_real`، `test_editor_save_both_sides` | كتالوج المالك ومجلده المنجز الحقيقي |
| `test_owner_studio` | `sudo apt install python3-tk` |
| `test_nutrition_read`، `test_nutrition_smart_v2`، `test_quality_date_v21` | نماذج OCR وأصول (tesseract + بيانات العربية) |

> **قاعدة ملزِمة:** إن حُجب أحدها فالنقص في البيئة لا في الكود. لا
> «تُصلحه» بتخفيف توقعاته — هيّئ البيئة (قسم 2). وقد ثبت هذا عمليًا:
> كل إخفاق حُقّق في جلستين متتاليتين كان في **أداة القياس** لا في
> التطبيق (باركودات بخانة تحقق EAN-13 خاطئة، تجويفة اختبار من 12 صنفًا
> فقط، وتوقّع ترقيم يخالف قاعدة المالك المعتمدة).

**استثناء مهم:** توقّع خاطئ في الاختبار يخالف قاعدة معتمدة **يُصحَّح**،
وهذا ليس تخفيفًا. مثاله في هذه الجلسة: `test_owner_units_real` كان يشترط
بدء الترقيم من `1` بينما القاعدة أن الرئيسية بلا رقم ثم `-2` — فصُحِّح
التوقّع لا التطبيق.

---

## 8. المشكلة 3 · **لم تُبدأ**

بلاغ المالك الثالث لم يُشخّص. **اسأل المالك عن تفصيله قبل أي عمل** —
لا تخمّن.

## 9. العمل المتبقي بالترتيب

1. **المشكلة 3**: استوضح البلاغ من المالك ثم شخّصه بالاختبار العملي
   قبل أي تعديل. **لا تخمّن** (قسم 8).
2. **البناء على ويندوز**: تُنجزه ورشة `.github/workflows/build-windows.yml`
   آليًا (PyInstaller ⇒ `installer.nsi` و`installer_owner.nsi`). وقبل
   PyInstaller **يجب** تشغيل `build/windows/توليد_بيانات_الإصدار.py`
   وهي خطوة مضمّنة في الورشة.
3. **التسليم**: الشفرة كاملة (كل `.py` والإعدادات و`requirements.txt`)
   لا ملف تثبيت مُجمَّع فقط.

### ما أُنجز في جلسة 2.9.9 الأخيرة

| العمل | الحصيلة |
| --- | --- |
| ربط الوحدات من الإكسل | مطابقة الوحدات **77/320 ⇒ 320/320** |
| توحيد مثبتات NSIS | 8 ملفات ⇒ **2** (المستخدم والمالك، برنامجان مختلفان) |
| توحيد ورشات البناء | 3 ورشات ⇒ **1**؛ وكانت العاملة تبني مُثبِّت 2.0.0 |
| بيانات إصدار PyInstaller | مولّد من `VERSION`؛ كان ملف المالك على 2.9.5 |
| اتفاقية الترخيص | حُذف «الإصدار 2.9.5» منها، وصُحِّحت نهايات الأسطر إلى CRLF |
| حاجز تناسق الإصدار | `test_version_consistency.py` — 28 فحصًا، ومُتحقَّق سلبيًا 8/8 |

## 10. قاعدة «لا تكرار» — أهم قاعدة في المشروع

> كل وظيفة تسكن مكانًا واحدًا. عند النقل يُحذف الأصل من الجذور — لا يُخفى
> ولا يُعطَّل ولا يُترك «للاحتياط».

الترقيع يمرّ عبر ثلاث طبقات: `native_app.py` يبني الواجهة، ثم `v2_ui.py`
و`native_app_v2.py` يرقّعانها في وقت التشغيل. إخفاء زر في الأولى **لا
يمنع** الثانية من إعادته — وهذا ما حدث فعلًا مع زر أداة التسمية.

```bash
grep -rn "اسم_الزر\|objectName\|دالة_الفتح" \
  windows_app/native_app.py windows_app/native_app_v2.py windows_app/v2_ui.py
```

يُحذف أربعة أشياء لا واحد: **الزر**، **دالة الفتح**، **الصنف/النافذة**،
**قواعد CSS**.

**محذوف سابقًا — لا تُعِده** (اختبارات تحرسه): `BulkRenameDialog`،
`open_rename_tool`، `v2_open_rename_tool`، `_open_bulk_rename`، زر «أداة
إعادة التسمية»، أنماط `v2RenameBtn` و`v2EditorBtn`.

## 11. قواعد المالك الملزِمة

- **راجعه قبل الإنهاء**: اعرض كل إصلاح عليه قبل تثبيته.
- **بلا ذكاء اصطناعي في التسليم**: فقط ما يمنع البرنامج من العمل بدونه.
- **الإكسل مرجع مطلق**: مطابقة 100%، وحتى الصنف ذو الصورة الواحدة تُقرأ
  وحدته من الإكسل. الصنف الغائب عن الإكسل يبقى بلا تغيير.
- **الواجهة**: المهم في الرئيسية، والمتقدم في نافذة منفصلة. رموز
  وتلميحات لا نصوص متراكمة. لا أرقام أبعاد صلبة — كل مقاس عبر `ui_scale`
  ليتكيّف حتى 800×600 بلا قصّ نص. عند تعديل رقم صنف يدويًا يبقى المستخدم
  في موضعه. اسم المنتج يُعرض كاملًا مهما طال.
- **التسمية**: خيارات كثيرة مرنة، قوالب جاهزة، وحفظ آخر اختيار.
- **الترخيص**: ربط المالك/المستخدم غير قابل للاختراق، مدد يحددها المالك،
  تفعيل أجهزة بموافقته، لا استرداد بعد الدفع.
  التواصل: `ahmadjookr06@gmail.com` — `0582381000`.
- **السرعة والخلو من الأخطاء** شرطان لا يُتنازل عنهما.

## 12. مصائد تجنّبها

- **لا تُخفّف توقعات اختبار** لتمريره. الفاشل بيئيًا يبقى فاشلًا حتى
  تتوفر بياناته.
- **التعليقات تذكر أسماء محذوفة عمدًا.** أي فحص نصي على المصدر يجب أن
  يُزيل التعليقات ونصوص التوثيق أولًا، وإلا فشل ظلمًا.
- **أنهِ خيوط Qt قبل هدم النوافذ** في أي اختبار واجهة، وإلا `SIGABRT`.
- **الترقيم يُنتج من سبعة مواضع.** أي تعديل على قاعدة التسمية يُفحص في
  كلها معًا لا في واحد.
